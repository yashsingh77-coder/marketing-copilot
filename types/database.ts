/**
 * PLACEHOLDER — regenerate after running the migration:
 *   npm run db:types
 *
 * The generated file will replace this with the full typed schema.
 * This stub keeps `createClient<Database>()` compiling until then.
 */
export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[];

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
